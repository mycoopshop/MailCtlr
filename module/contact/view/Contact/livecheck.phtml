<div class="container">
    <h2>Premi il tasto START per iniziare il processo di pulizia dei contatti!</h2>
    <h4>Verifiche da fare: <b><?=@$data->totale;?></b> - Numero di verifiche rimaste: <b id="remain"><?=@$data->number;?></b></h4>
    <br />
    <div class="row">
        <div class="col-sm-2">
            <label>Elimina duplicati</label>
            <select class="form-control" id="duplicate" name="duplicati">
                <option value="1">SI</option>
                <option value="0" selected="">NO</option>
            </select>
        </div>
        <div class="col-sm-2">
            <label>Elimina non attivi</label>
            <select class="form-control" id="active" name="active">
                <option value="0" selected="">NO</option>
                <option value="1">SI</option>
            </select>
        </div>
        <div class="col-sm-2">
            <label>Processa per volta</label>
            <input type="number" class="form-control" id="limit" name="limit" value="1" />
        </div>
        <div class="col-sm-2">
            <label>Inserisci in</label>
            <select class="form-control" id="lista" name="lista">
                <option value="0" selected="">NESSUNA</option>
                <?php foreach($data->liste as $ls){?>
                <option value="<?=$ls->id?>"><?=$ls->nome?></option>
                <?php } ?>
            </select>
        </div>
        <div class="col-sm-2">
            <label>Richiesta privacy</label>
            <select class="form-control" id="privacy" name="privacy">
                <option value="0" selected="">NO</option>
                <option value="1">SI</option>
            </select>
        </div>
        <div class="col-sm-2">
            <label>Email privacy</label>
            <select class="form-control" id="p_email" name="p_email">
                <option value="0" selected="">NESSUNA</option>
                <?php foreach($data->email as $em){?>
                <option value="<?=$em->id?>"><?=$em->oggetto?></option>
                <?php } ?>
            </select>
        </div>
    </div>
    <!-- START / STOP BUTTON -->
    <div class="row">
        <div class="col-sm-1 col-sm-offset-11">
            <label>&nbsp;</label>
            <button 
                type="button" 
                id="start"
                data-placement="bottom" 
                data-toggle="tooltip" 
                data-original-title="Torna alla scermata precedente" 
                class="navbar-btn btn btn-success" 
                onClick="process( '<?=$data->action?>' , <?=$data->number?> , <?=@$data->totale?> );" >
                <span class="glyphicon glyphicon-play"></span> START
            </button>
            <button 
                type="button" 
                id="stop"
                style="display:none;"
                data-placement="bottom" 
                data-toggle="tooltip" 
                data-original-title="Torna alla scermata precedente" 
                class="navbar-btn btn btn-danger" 
                onClick="process_stop();" >
                <span class="glyphicon glyphicon-stop"></span> Stop
            </button> 
        </div>
    </div>
    <!-- PROGRESS BAR ACTIVITY -->
    <div class="row">
        <div class="col-sm-12">
            <div class="progress">
                <div 
                    id = "progress"
                    class="progress-bar progress-bar-striped active" 
                    role="progressbar" 
                    aria-valuemax="100" 
                    style="width: 0%">
                        0% Complete
                </div>
            </div>
        </div>
    </div>
    <br />
    <textarea rows="20" readonly="readonly" class="result form-control"></textarea>
</div>